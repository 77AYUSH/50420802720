import React from 'react';
import ReactDOM from 'react-dom';
import TrainScheduleApp from './components/TrainScheduleApp';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <TrainScheduleApp />
  </React.StrictMode>,
  document.getElementById('root')
);